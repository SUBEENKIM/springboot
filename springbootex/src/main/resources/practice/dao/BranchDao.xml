<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="practice.dao.BranchDao">

  <resultMap type="branch" id="branchMap">
    <id column="bno" property="no"/>
    <result column="bname" property="name"/>
    <result column="baddr" property="addr"/>
    <result column="btel" property="tel"/>
    <result column="bintro" property="intro"/>
    <result column="barea" property="area"/>
    <result column="blilg" property="lilg"/>
    <result column="bmno" property="bmno"/>
    <result column="bmname" property="bmname"/>
    <collection property="photoList" ofType="string">
      <result column="path"/>
    </collection>
  </resultMap>

  <select id="selectList" resultMap="branchMap" parameterType="map">
    select  b.bno, b.bname, b.btel, bm.bmno, bm.bmname, null as path
    from branch b left join branch_master bm
    on b.bmno=bm.bmno
    limit #{startIndex}, #{pageSize}
  </select>
  
  <select id="selectOne" resultMap="branchMap" parameterType="int">
    select  b.bno, b.bname, b.btel, b.baddr, b.bintro, b.barea, b.blilg, bm.bmno, bm.bmname, bp.path
    from branch b left join branch_master bm on b.bmno=bm.bmno 
    			  left outer join branch_phot bp on b.bno=bp.bno
    
    where b.bno = #{branchNo}
  </select>
  
  
  <insert id="insert" parameterType="branch">
    insert into branch(bname, btel, baddr, barea, blilg, bmno, bintro) 
    values(#{name}, #{tel}, #{addr}, #{area}, #{lilg}, #{bmno}, #{intro})
  </insert>
  
  <delete id="delete" parameterType="int">
    delete from branch where bno=#{branchNo}
  </delete>
  
  <update id="update" parameterType="branch">
    update branch set 
      bname=#{name}, 
      btel=#{tel},
      baddr=#{addr},
      blilg=#{lilg},
      bintro=#{intro},
      barea=#{area}
    where bno=#{no}
  </update>
  
  <insert id="insertPhoto" parameterType="map">
    insert into branch_phot(bno,path)
    values(#{branchNo},#{photoPath})
  </insert>
  
  <select id="selectPhotoList" parameterType="int">
    select bpno, bno, path
    from branch_phot
    where bno=#{branchNo}
  </select>
  
  <delete id="deletePhoto" parameterType="int">
    delete from branch_phot
    where bno=#{branchNo}
  </delete>

</mapper>